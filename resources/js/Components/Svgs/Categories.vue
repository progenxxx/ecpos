<!-- <script setup>

const props = defineProps({
    showThis: {
        type: Boolean,
        default: false,
    },
    categoryName: {
        type: String,
    },
    numResults: {
        type: Number,
    }
});

</script>

<template>
    <div v-show="showThis" class="container-fluid mt-5" >
        <div class="d-flex flex-column">
            <h5 class="fw-bold ">{{ categoryName }}</h5>
            <small class="text-secondary ms-2">{{ numResults > 0 ? (numResults > 1 ? numResults+' Results' : numResults+' Result') : 'No Result' }}</small>
        </div>
        <div class="row">
            <slot></slot>
        </div>
    </div>
</template>
 -->

 <template>
  <svg fill="#ffffff" viewBox="0 0 35 35" data-name="Layer 2" id="e73e2821-510d-456e-b3bd-9363037e93e3" xmlns="http:
</template>